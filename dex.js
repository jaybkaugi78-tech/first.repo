fetch(`qhfij`).then( (r) => r. json())
const y = await fetch(`qhfij`).then( (r) => r. json())

const x = new Promise(resolve, reject)


g().then(get).then()


Promise.resolve(`hey`)

async function gr(){
    try {
        await fetch(`qhfij`)

    } catch (error) {

    }
}

